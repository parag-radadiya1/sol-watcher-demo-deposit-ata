export const REGEX = {
  companyName: /^[a-zA-Z0-9 ]*$/,
  userName: /[a-zA-Z]/,
  partyName: /^[a-zA-Z0-9]+$/,
  objectId: /^[0-9a-fA-F]{24}$/,
  file: /[/\s]+/g,
};

export const PAGINATION_CONFIG = {
  page: 1,
  limit: 0,
};
